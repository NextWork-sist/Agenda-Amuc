let reservasGlobal=[];let currentCalendarDate=new Date();
function money(v){return new Intl.NumberFormat('es-AR',{style:'currency',currency:'ARS',maximumFractionDigits:0}).format(Number(v||0));}
function fd(v){return v?new Intl.DateTimeFormat('es-AR').format(new Date(v+'T12:00:00')):'';}
function esc(v=''){return String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');}
function renderCalendar(){const y=currentCalendarDate.getFullYear(),m=currentCalendarDate.getMonth();const t=new Intl.DateTimeFormat('es-AR',{month:'long',year:'numeric'}).format(new Date(y,m,1));document.getElementById('calendarTitle').textContent=t.charAt(0).toUpperCase()+t.slice(1);const g=document.getElementById('calendarGrid');g.innerHTML='';const first=new Date(y,m,1).getDay(),days=new Date(y,m+1,0).getDate(),prev=new Date(y,m,0).getDate();for(let i=0;i<42;i++){let d,dt,out=false;if(i<first){d=prev-first+i+1;dt=new Date(y,m-1,d);out=true}else if(i>=first+days){d=i-first-days+1;dt=new Date(y,m+1,d);out=true}else{d=i-first+1;dt=new Date(y,m,d)}const iso=[dt.getFullYear(),String(dt.getMonth()+1).padStart(2,'0'),String(dt.getDate()).padStart(2,'0')].join('-');const rr=reservasGlobal.filter(r=>r.fecha===iso&&r.estado!=='CANCELADA');const cell=document.createElement('div');cell.className='calendar-cell'+(out?' outside-month':'');const events=rr.map(r=>{const c=r.clientes?`${r.clientes.nombre||''} ${r.clientes.apellido||''}`.trim():'';const h=r.hora_inicio?r.hora_inicio.slice(0,5):'';const lab=[h,r.tipo_evento||'Evento',c].filter(Boolean).join(' · ');return `<div class="calendar-event ${r.estado==='CONFIRMADA'?'confirmed':'pre'}" title="${esc(lab)}">${esc(lab)}</div>`}).join('');cell.innerHTML=`<div class="calendar-day-number">${d}</div><div class="calendar-events">${events}</div>`;g.appendChild(cell)}}
document.addEventListener('DOMContentLoaded',async()=>{const c=await requireSession();if(!c)return;document.getElementById('welcomeText').textContent=`Bienvenido, ${c.perfil.nombre} ${c.perfil.apellido||''}`.trim();const[{data:r},{data:cl},{data:p}]=await Promise.all([supabaseClient.from('reservas').select('id,fecha,hora_inicio,hora_fin,tipo_evento,valor_total,estado,clientes(nombre,apellido)').order('fecha'),supabaseClient.from('clientes').select('id'),supabaseClient.from('pagos').select('importe')]);reservasGlobal=r||[];const ps=p||[];document.getElementById('statReservas').textContent=reservasGlobal.length;document.getElementById('statClientes').textContent=(cl||[]).length;document.getElementById('statTotalReservado').textContent=money(reservasGlobal.reduce((s,x)=>s+Number(x.valor_total||0),0));document.getElementById('statCobrado').textContent=money(ps.reduce((s,x)=>s+Number(x.importe||0),0));document.getElementById('prevMonthBtn').onclick=()=>{currentCalendarDate=new Date(currentCalendarDate.getFullYear(),currentCalendarDate.getMonth()-1,1);renderCalendar()};document.getElementById('nextMonthBtn').onclick=()=>{currentCalendarDate=new Date(currentCalendarDate.getFullYear(),currentCalendarDate.getMonth()+1,1);renderCalendar()};renderCalendar();const hoy=new Date().toISOString().slice(0,10),prox=reservasGlobal.filter(x=>x.fecha>=hoy&&x.estado!=='CANCELADA').slice(0,8),box=document.getElementById('proximasReservas');box.innerHTML=!prox.length?'<p class="muted">Todavía no hay próximas reservas.</p>':`<table><thead><tr><th>Fecha</th><th>Cliente</th><th>Evento</th><th>Estado</th><th>Valor</th></tr></thead><tbody>${prox.map(x=>`<tr><td>${fd(x.fecha)}</td><td>${x.clientes?`${x.clientes.nombre||''} ${x.clientes.apellido||''}`:'-'}</td><td>${x.tipo_evento||'-'}</td><td><span class="badge">${x.estado}</span></td><td>${money(x.valor_total)}</td></tr>`).join('')}</tbody></table>`});
